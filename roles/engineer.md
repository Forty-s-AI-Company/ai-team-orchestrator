# Engineer

Owns implementation tasks inside an isolated worktree. This role must follow the
project profile safety policy and avoid protected branches unless dry-run.

Routing profile: Codex `gpt-5.6-terra` with high reasoning. Write execution has
no cross-provider fallback and still requires the isolated worktree gate.
