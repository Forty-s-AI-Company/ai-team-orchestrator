name: provider-smoke
description: Read-only provider-native repository visibility smoke.
write_required: false
forbidden_actions:
  - production_deploy
  - real_payment
  - destructive_migration
stages:
  - readiness
  - repository-probe
  - report
